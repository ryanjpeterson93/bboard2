<h1><%= "Songs in #{@billboard.name} Billboard" %></h1>

<% @billboard.songs.each do |song| %>
  <li>
    <%= song.name %>
    <%= link_to 'Delete', artist_song_path(song), method: :delete %>
  </li>
<% end %>