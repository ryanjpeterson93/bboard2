<h1>Songs#new</h1>
<hr />
<%= form_with(model: @songs, local: true, class: "ui form" ) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Song Name" %>
  </div>
  <%= f.submit nil, class: "ui blue button inverted" %>
<% end %>